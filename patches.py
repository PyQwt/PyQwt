patches = {'.debug': '--- qwt-sources/include/qwt_text.h.debug\tSun Mar 14 20:51:37 2004\n+++ qwt-sources/include/qwt_text.h\tSun Mar 14 20:51:37 2004\n@@ -37,6 +37,7 @@\n public:\n     virtual ~QwtText();\n \n+#ifndef PYQWT_DEBUG\n     static QwtText *makeText(const QString &, Qt::TextFormat,\n         int align, const QFont &, const QColor &color = QColor(),\n         const QPen &pen = QPen(Qt::NoPen),\n@@ -46,6 +47,17 @@\n         int align, const QFont &, const QColor &color = QColor(),\n         const QPen &pen = QPen(Qt::NoPen),\n         const QBrush &brush = QBrush(Qt::NoBrush));\n+#else\n+    static QwtText *makeText(const QString &, Qt::TextFormat,\n+        int align, const QFont &, const QColor &color = QColor(),\n+        const QPen &pen = QPen(Qt::red, 2),\n+        const QBrush &brush = QBrush(Qt::yellow));\n+\n+    static QwtText *makeText(const QString &, \n+        int align, const QFont &, const QColor &color = QColor(),\n+        const QPen &pen = QPen(Qt::red, 2),\n+        const QBrush &brush = QBrush(Qt::yellow));\n+#endif\n \n     static void setDefaultFormat(Qt::TextFormat);\n     static Qt::TextFormat defaultFormat();\n', '.autoscale': '--- qwt-sources/src/qwt_plot.cpp.autoscale\tSun Mar 14 20:51:37 2004\n+++ qwt-sources/src/qwt_plot.cpp\tSun Apr  4 21:57:54 2004\n@@ -9,6 +9,7 @@\n \n // vim: expandtab\n \n+#include <float.h>\n #include <qlabel.h>\n #include <qpainter.h>\n #include <qfocusdata.h>\n@@ -474,6 +475,47 @@\n         }\n     }\n \n+    // PyQwt adjust the autoscalers also for the markers\n+\n+    double min[axisCnt];\n+    double max[axisCnt];\n+    for (i = 0; i < axisCnt; i++)\n+    {\n+        min[i] = DBL_MAX;\n+        max[i] = DBL_MIN;\n+    }\n+    \n+    QwtPlotMarkerIterator itm = markerIterator();\n+    for (const QwtPlotMarker *m = itm.toFirst(); m != 0; m = ++itm)\n+    {\n+        const int xAxis = m->xAxis();\n+        const int yAxis = m->yAxis();\n+        const double x = m->xValue();\n+        const double y = m->yValue();\n+\n+        if (x<min[xAxis])\n+            min[xAxis] = x;\n+        if (x>max[xAxis])\n+            max[xAxis] = x;\n+        if (y<min[yAxis])\n+            min[yAxis] = y;\n+        if (x>max[yAxis])\n+            max[yAxis] = y;\n+    }\n+\n+    for (i = 0; i < axisCnt; i++)\n+    {\n+        if ( (min[i] != DBL_MAX) && (max[i] != DBL_MIN) )\n+        {\n+            if ( !resetDone[i] && (min[i] != max[i]) )\n+            {\n+                d_as[i].reset();\n+                resetDone[i] = TRUE;\n+            }\n+            d_as[i].adjust(min[i], max[i]);\n+        }\n+    }\n+    \n     //\n     // Adjust scales\n     //\n@@ -580,6 +622,51 @@\n         const QwtArray<QwtDiMap> &map, const QwtPlotPrintFilter &pfilter) const\n {\n     //\n+    // draw scales\n+    //\n+\n+    for ( int axis = 0; axis < axisCnt; axis ++ )\n+    {\n+\tif ( d_axisEnabled[axis])\n+\t{\n+\t    QwtScaleDraw *sd = (QwtScaleDraw *)d_scale[axis]->scaleDraw();\n+\t    int x = sd->x();\n+\t    int y = sd->y();\n+\t    int length = sd->length();\n+\t    QwtScaleDraw::Orientation orientation = sd->orientation();\n+\t    switch (orientation)\n+\t    { \n+\t    case QwtScaleDraw::Bottom:\n+\t\tsd->setGeometry(\n+\t\t    map[axis].transform(sd->d1()),\n+\t\t    map[QwtPlot::yLeft].transform(d_scale[QwtPlot::yLeft]->scaleDraw()->d1()),\n+\t\t    length, orientation);\n+\t\tbreak;\n+\t    case QwtScaleDraw::Top:\n+\t\tsd->setGeometry(\n+\t\t    map[axis].transform(sd->d1()),\n+\t\t    map[QwtPlot::yLeft].transform(d_scale[QwtPlot::yLeft]->scaleDraw()->d2()),\n+\t\t    length, orientation);\n+\t\tbreak;\n+\t    case QwtScaleDraw::Left:\n+\t\tsd->setGeometry(\n+\t\t    map[QwtPlot::xTop].transform(d_scale[QwtPlot::xTop]->scaleDraw()->d1()),\n+\t\t    map[axis].transform(sd->d2()),\n+\t\t    length, orientation);\n+\t\tbreak;\n+ \t    case QwtScaleDraw::Right:\n+\t\tsd->setGeometry(\n+\t\t    map[QwtPlot::xTop].transform(d_scale[QwtPlot::xTop]->scaleDraw()->d2()),\n+\t\t    map[axis].transform(sd->d2()),\n+\t\t    length, orientation);\n+ \t\tbreak;\n+\t    }\n+\t    sd->draw(painter);\n+\t    sd->setGeometry(x, y, length, orientation);\n+\t}\n+    }\n+\n+    //\n     // draw grid\n     //\n     if ( pfilter.options() & QwtPlotPrintFilter::PrintGrid )\n@@ -592,7 +679,7 @@\n     }\n \n     //\n-    //  draw curves\n+    // draw curves\n     //\n     QwtPlotCurveIterator itc = curveIterator();\n     for (QwtPlotCurve *curve = itc.toFirst(); curve != 0; curve = ++itc )\n'}
